{
  "strong_write": [
    "Within the fixed 24-node circuit, `L2H14` is the earliest retained head-level reader that feeds `MLP11`.",
    "`L2H14` reads the instruction opening on the lead_phrase side rather than the file-target / function-body anchors that dominate `L20H5`.",
    "Within-opening matched counterfactuals show that `L2H14` is better described as an opening-frame reader than as a fully abstract delivery-semantic reader.",
    "`L2H14 -> MLP11` is the only non-zero earliest-candidate transmission into `MLP11`; the other priority candidates are downstream carriers or routers.",
    "`MLP11` is the first stable semantic scaffold writer, and `MLP11 -> MLP16 -> MLP19` is the earliest strong scaffold segment.",
    "The full chain `L2H14 -> MLP11 -> MLP16 -> MLP19 -> L20H5 -> L21H1/L21H12 -> L24H6 -> MLP27` reaches rescue `0.998`, tool_top1 `0.989`, boundary `1.000` on 1722 samples."
  ],
  "medium_write": [
    "The read-side signal is strongest in `L2H14 K`, while the part that survives into `MLP11` is preserved in both `K` and `Z`; `Q` is effectively zero and `V` is net negative.",
    "Natural clean lines show a weak semantic skew already at `L2H14 lead_k`, but matched opening variants do not cluster by semantic class there."
  ],
  "weak_write": [
    "The exact object-language decomposition inside that opening bundle is fully solved."
  ],
  "paper_grade_status": {
    "chain_identity_and_transmission": true,
    "earliest_reader_exact_object_semantics": false,
    "overall": false
  }
}
