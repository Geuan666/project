Create a publication-style scientific mechanism diagram for mechanistic interpretability research in large language models.

Canvas: portrait 9:16 layout, clean white background, professional NeurIPS / ICML paper figure style, crisp vector-like shapes, flat modern colors, no decorative clutter, no fake 3D rendering.

The figure should explain a tool-use decision mechanism in an agentic language model. It should look like a polished paper figure draft, not a marketing poster.

Overall structure:
1. Top half: a simplified circuit view with colored nodes and arrows showing information flow.
2. Bottom half: four large module boxes arranged left-to-right and connected by arrows:
   Instruction Integration -> Output-Route Decision -> Tool-Call Construction
   and in parallel from Output-Route Decision downward/right to Tool-Call Suppression.

Visual idea:
- The top circuit should show a sparse neural circuit made of circles or rounded rectangles connected by arrows.
- Use four consistent colors to indicate which circuit nodes belong to which module:
  blue for Instruction Integration,
  orange for Output-Route Decision,
  green for Tool-Call Construction,
  red for Tool-Call Suppression.
- Some nodes should be colored orange and labeled clearly with:
  MLP11, MLP16, MLP19
  These three orange nodes should form the central spine in the circuit.
- Add a fork from the orange spine:
  one branch toward green construction nodes,
  one branch toward red suppression nodes.

What the figure should communicate:
- The model first integrates distributed instruction cues.
- Then it forms an internal output-route decision.
- That decision is implemented by a central orange spine:
  MLP11 -> MLP16 -> MLP19
- From there, the state fans out in parallel:
  toward tool-call construction
  and toward tool-call suppression / direct-answer route.

Important scientific content to encode visually:
- MLP11 should look like the earliest orange node where route state first appears.
- MLP16 should look like the strongest shared hub / amplifier in the orange chain.
- MLP19 should look like a late orange fanout hub that distributes the state to downstream branches.
- A strong branch should go from MLP16 to the red suppression pathway.
- Multiple weaker outgoing arrows should leave MLP19 toward the green and red downstream receivers, showing parallel fanout rather than a single bottleneck.

Labels to include, with clean readable typesetting:
- Instruction Integration
- Output-Route Decision
- Tool-Call Construction
- Tool-Call Suppression
- MLP11
- MLP16
- MLP19
- tool route
- direct-answer route

Style constraints:
- The text should be minimal, clear, and readable.
- Avoid dense paragraphs inside the image.
- Use thin arrows, clean alignment, generous whitespace.
- Make it feel like a serious mechanism figure for a paper.
- No logos, no watermarks, no extra irrelevant text.

The final image should look like a first-draft mechanism figure that a researcher could refine for a conference paper.
