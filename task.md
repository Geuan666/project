# 当前任务

## 固定前提

- 当前统一使用的模型是 `/root/autodl-tmp/Qwen/Qwen3-1.7B`。
- 当前固定的总机制假说见 [AGENTS.md](/root/autodl-tmp/project/AGENTS.md) 和 [history.md](/root/autodl-tmp/project/history.md) 中的英文原文。

## 总原则

后续工作顺序不等于机制图从左到右的顺序。为了让叙事先站稳，再往两边扩展，当前推荐的执行顺序是：

1. `Output-Route Decision`
2. `Instruction Integration`
3. `Tool-Call Construction`
4. `Tool-Call Suppression`

每一步都要输出同样的东西：

- 模块边界
- 核心节点
- 主要读入对象
- 主要写出状态
- 向下游模块的传递证据
- 模块级充分性和必要性证据

## 第一步：Output-Route Decision

先把中间的决策模块做扎实。

目标：

- 确定哪些节点真正构成“走工具路线还是走直接回答路线”的稳定内部决定。
- 证明这个模块不是模糊相关，而是真的会驱动后面两条路线。

当前优先节点：

- `MLP11`
- `MLP16`
- `MLP19`

需要回答的问题：

- 这个模块里到底写出的是什么状态？
- 这个状态是否能稳定传到工具路线和抑制路线？
- 该模块单独拿出来，是否足以明显改变首个输出词的方向？

## 第二步：Instruction Integration

在决策模块站稳后，再回过头解释模型前面是怎么整合提示词的。

目标：

- 解释模型如何把开头要求、函数体、文件名和后半句说明放在一起理解。
- 不再把前端解释成单个头，而是允许它成为一个分布式整合模块。

当前优先节点：

- `L2H14`
- 结合注意力头实验继续筛选出的 instruction-binding 头

需要回答的问题：

- 哪些节点会把不同位置的提示信息绑在一起？
- 哪些节点只是早期 reader，哪些节点真的把信息送进决策模块？

## 第三步：Tool-Call Construction

在决策模块和前端整合模块明确后，再解释工具路线是怎么把 `<tool_call>` 组装出来的。

目标：

- 解释工具路线如何组织文件名、函数体和调用格式。
- 明确哪些节点是主干，哪些是支路。

当前优先节点：

- `L20H5`
- `L21H12`
- `L24H6`
- `MLP27`
- `L21H1` 作为并行或辅助候选

需要回答的问题：

- 谁在读文件名？
- 谁在读函数体？
- 谁在读调用格式？
- 谁把状态真正写向 `<tool_call>`？

## 第四步：Tool-Call Suppression

最后把直接回答一侧的竞争路线做完整。

目标：

- 说明不走工具路线时，模型如何写强 `no_tool`，并同时压住工具调用路线。

当前优先节点：

- `L16H4`
- `MLP17`
- `L23H6`

需要回答的问题：

- 谁在读普通回答线索？
- 谁在写 `no_tool` 状态？
- 谁在把 suppressive state 送到输出附近？
- 它是只抬高 `no_tool`，还是同时压低工具路线，还是两者同时做？
